<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="AppCherrys.Views.Actas.NuevaActaView"
             xmlns:local="clr-namespace:AppCherrys.Constantes.Localization">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="{x:Static local:Messages.Cancelar}" Clicked="Cancelar_Clicked" />
        <ToolbarItem Text="{x:Static local:Messages.Guardar}" Clicked="Guardar_Clicked" />
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <StackLayout Spacing="20" Padding="15">
            <Label Text="{x:Static local:Messages.Titulo}" FontSize="Medium" />
            <Entry Text="{Binding Item.Titulo}" d:Text="Titulo" FontSize="Small" />
            <Label Text="{x:Static local:Messages.Descripcion}" FontSize="Medium" />
            <Editor Text="{Binding Item.Descripcion}" d:Text="Descripción" FontSize="Small" Margin="0" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>