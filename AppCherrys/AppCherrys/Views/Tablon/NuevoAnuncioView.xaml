<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="AppCherrys.Views.Tablon.NuevoAnuncioView"
             Title="Nuevo anuncio">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Cancelar" Clicked="Cancelar_Clicked"/>
        <ToolbarItem Text="Guardar" Clicked="Guardar_Clicked" />
    </ContentPage.ToolbarItems>
    
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>
            <StackLayout Spacing="20" Padding="15" Grid.Row="0">
                <Entry Text="{Binding Item.Titulo}"  FontSize="Small" Placeholder="Titulo"/>
                <Editor Text="{Binding Item.Descripcion}"  FontSize="Small" Margin="0" AutoSize="TextChanges" Placeholder="Descripcion"/>
            </StackLayout>
            <StackLayout Grid.Row="1" Orientation="Horizontal">
                <Button Text="Adjuntar imagen"/>
                <Button Text="Adjuntar Gif"/>
                <Button Text="Adjuntar Encuesta"/>
            </StackLayout>
        </Grid>
    </ContentPage.Content>
    
</ContentPage>
